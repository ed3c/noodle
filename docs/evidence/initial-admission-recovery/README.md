# Initial admission recovery evidence

These are non-authorizing observations for Noodle #84. `receipt.json` records the
source candidate, actual original order, controls and their limits; `manifest.json`
binds copied raw evidence. The branch containing these artifacts publishes evidence
and does not replace the product PR or its provider/readback authority.

The original writer ran before implementation commit. Both blocked attempts are
retained, including sandbox refusal and the observed loss of dirty work on retry.
The supervisor restored saved bytes, verified and committed the repair, then used
the original order for a fresh bounded CLI/typed-outcome continuation.

For each of 19 gate controls, read the planted defect RED and the same restored
control GREEN plus the shared legal non-case GREEN. The first schema mutant only
failed compilation and is explicitly invalid as RED; its corrected mutant is under
`mutation-schema-correction`. Correct refusals are GREEN, never defect RED.

The fixed external observer drives a copy of the preserved Soodles failure. It
verifies original bytes, exact owner next argv, canonical immutability and repeated
readback. Refusal cases assert the specific cause, not merely nonzero exit. Source
selection revisions and prior observer errors are preserved.

The P consumer's actual native events and exit receipts are retained. One run
completed all CLI operations but was stopped with SIGTERM after its deadline;
that is not a natural successful model exit. Later original-order evidence must
stand independently. No experiment result grants provider delivery authority.

Provider merge, closure, original-order completion and teardown are separate raw
readbacks. A pending receipt or successful Test run does not itself resolve #84.
